# Pronghorn platform — comprehensive status (7/31, after ~10 days down)

_All three lanes idle since ~7/21 (context deaths + John focused elsewhere; PM
loop also stopped). Everything below verified against the live DB/site 7/31 —
not from lane self-reports._

## ✅ LIVE ON THE SITE (shipped + deployed, incl. today's catch-up merge)

**Lists / UX (John's "Excel-like" standard — Lane B's final rollout MERGED +
DEPLOYED TODAY):** every list (Companies, Contacts, Brokers, Listings, Deals,
Enrichment, River Guides) now has: sort on EVERY column · a NAMED filter
dropdown on every filterable column (the invisible-filter bug is dead) ·
Email/Phone/LinkedIn as real value columns with per-channel has/missing
filters · URL-persistent state · sticky h-scrollbar · full-width tables.

**River Guides channel (end-to-end):** 467 guides · 188 with a contact
channel · 115 Call-now · 25 status-verified · 78 in the paid/VA queue.
Discovery sweep (hallucination-guarded), tier-1 enrichment with live progress
banner + clickable run history + cost-before-click, verify-evidence expandable
on each row, CRM cross-links (contacts tagged river_guide + former companies).

**Size estimation:** payroll-% is THE input (page rebuilt); PPP + non-PPP
ensemble = ~100% of on-target base sized; ~Rev/~EBITDA columns on every list.

**Costs:** Month + YTD windows (same breakdown), Upwork VA manual entry
("Log a cost"), subs start_date=7/1 (no more run-rating back to Jan), full
Costs page reachable (sidebar scroll fix + badge link).

**Outlook deal-tracking:** classifier + deal_proposals (0019) + approve/dismiss
UI on Key Actions; Lane C fixed 2 classifier bugs before standing down.

**Data intake portal (/intake):** upload CSV/xlsx of contacts/companies/
river-guides/VA-enrichment → Claude column-mapping → preview → confirm →
routed with provenance. Built for Tom; needs a real-file shakedown test.

**Deals added by PM by hand:** Monster Tree/Roslyn Ridge (Kris Peck, CIM
Received) · NatureScapes (Kasper-brokered, full financials, CIM Received) ·
Kasper & Associates broker set (5 contacts w/ verified emails).

## ❌ BROKEN / NOT DONE (the honest list)

1. **Asking-price parse gap — WORSE (Lane A never picked it up; dead since
   7/20).** Fresh 7/31 audit: **Tupelo 0% asking (0/20, growing)** ·
   **DealRelations 0% everything (0/5)**. Also thin: BizQuest rev/cf/mult 0% ·
   BizBuySell rev 0% · Transworld rev 0% · LINK rev/cf 6% · BizBen ask 95%/
   mult 33%. → Lane A's #1 (queued 7/21, untouched).
2. **All three lanes offline ~10 days.** Fresh pastes needed (prompts below /
   in chat).
3. **River-guide OUTREACH is the missing last mile:** contact channels exist
   on-page, but "reach out" = manual mailto/tel. No approved river-guide
   template → no drafts. (Auto-draft engine exists but is rules-gated and the
   sample-review card is PARKED by John.)
4. **Tracerfy phones for guides: dead end as-is** — Lane C probed it; the
   batch person-mode needs a STREET ADDRESS, which guides don't carry.
   Options: enrich guide street addresses first, or lean on the VA for phones.
5. **YTD variable spend** still counts pre-July usage_events (subs fixed;
   variable YTD window starts Jan 1 by design — confirm John wants variable
   also to start 7/1).
6. **Intake portal untested with a real VA file.**

## 📥 NEEDS FROM JOHN
1. Paste the 3 lane restarts (full prompts in chat + RESTART-PROMPTS.md).
2. River-guide outreach template: approve one (equity-not-fees positioning,
   spec §8) so drafts can queue — or keep outreach manual for now.
3. VA workflow check: is the Upwork VA live? First NEEDS_PAID batch (78
   guides) is CSV-ready; VA results come back through /intake.
4. Confirm: should YTD *variable* spend also start 7/1 (like subs)?
5. Walk the river-guide flow (steps in chat) and send edits.

## 💡 SUGGESTED NEXT EDITS (PM's list, prioritized)
1. Asking-price/financials parse fixes (Lane A #1) + Market-Multiples
   time-series once coverage is real.
2. "Draft intro" button on river-guide rows → rules-gated outbox draft w/ the
   equity pitch (unlocks the last mile; needs John's template approval).
3. VA round-trip hardening: one-click "Send to VA" export (instructions
   embedded) + intake receipt that auto-fills the guide rows; track VA hit
   rate + $ per contact found on the Costs page.
4. Guide street-address enrichment (registered-agent / SoS data) to unlock
   the Tracerfy phone tier ($0.02/hit) as a cheaper-than-VA phone path.
5. Raise nightly verify cap on CALL_NOW guides (25/467 verified is the
   bottleneck to outreach-eligible).
6. Mobile card-collapse pass (Lane B's queued next unit).
7. Deal-proposals visibility: pending-proposal count badge in sidebar/Key
   Actions so Outlook-detected next-steps don't sit unseen.
8. Intake portal shakedown with a real VA file (PM can simulate one first).
